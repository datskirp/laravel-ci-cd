on:
  push:
    branches:
      - main
name: CI/CD
jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - name: Install composer dependencies
        run: |
          composer update
          composer install --no-scripts
      - name: Prepare Laravel Application
        run: |
          cp .env.ci .env
          php artisan key:generate
          php artisan migrate
      - name: Run Testsuite
        run: vendor/bin/phpunit tests/
  aws-deploy:
    needs: phpunit
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Run the build process with Docker
        uses: addnab/docker-run-action@v3
        with:
          image: localstack/localstack:latest
          options: -v ${{ github.workspace }}:/opt/code/localstack/project -p 4566:4566
          run: |
            ls -la
            curl http://localhost:4566/health
            #awslocal s3 mb s3://task15
            #awslocal s3 ./project s3://task15
            #awslocal ec2 run-instances --image-id ami-05f3141013eebdc12 --count 1 --instance-type t2.micro
            awslocal ec2 describe-images


